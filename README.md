# Moon Light API (Backend)

## Startup

1. Install dependencies:
```powershell
npm install
```

2. Start development server:
```powershell
npm run dev
```

Dev entrypoint: `src/index.ts` (`tsx watch`).

## Other Scripts

- Build: `npm run build`
- Start built app: `npm run start`
- Run tests: `npm run test`
